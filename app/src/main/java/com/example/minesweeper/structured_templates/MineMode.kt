package com.example.minesweeper.structured_templates

// Reveal mode properties for the mine field
enum class MineMode {
    FLAG, // Flag mode -> Place flags on the mine field, or remove already placed flags
    REVEAL // Reveal mode -> Reveal the mine field cell
}
